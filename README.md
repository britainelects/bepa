# GitHub Pages - Hosted JSON

This repository hosts a JSON file (data.json) via GitHub Pages.

How to use:
1. Edit `data.json` with your content.
2. Enable GitHub Pages in the repository Settings → Pages and choose the branch `main` and root (/) as the folder.
3. After I push the files the file will be available at:
   - Raw URL: https://raw.githubusercontent.com/britainelects/bepa/main/data.json
   - Pages URL (once Pages is enabled): https://britainelects.github.io/bepa/data.json
